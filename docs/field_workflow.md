# 比赛现场快速流程

1. `git status` 确认干净并记录最后可用 commit。
2. 只接 MaixCAM，确认焦距、曝光、颜色 mask、ROI 和 FPS。
3. 断开 DCC 动力，只接 UART 逻辑与共地，核对 `$MV,...#`。
4. 接 DCC 动力，无负载以低速点动两轴，确认轴、方向、限位和停止。
5. 先开环观察误差符号，再以小死区/低速闭环；测试丢帧时两轴会停止。
6. 现场调参一次只改一组 YAML，采样、回放、比较分数、commit。
7. 出现不可解释回归立即回滚到最后可用 commit，不在赛前重写架构。

