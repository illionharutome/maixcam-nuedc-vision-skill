# Serial logs

`scripts/tune.py log` appends parsed `$MV,AIM` records to `logs/serial/*.jsonl`. Generated logs are ignored by Git. Keep logs from different lighting, distance, background, and camera settings in separate files.
