# 2025E target aiming 索引

2025E 已验证代码当前仍保留在仓库根目录的 `apps_atomic/`、`apps_replay/`、`comm/`、`core/`、`vision/`、`scripts/`、`mcu_common/` 和 `mcu_stm32zet6_debug/`。

本次不复制或搬移这些文件，以免破坏 MaixCAM 闭环、analyze-raw、USART1 自动检查和 parser/state machine 基线。后续迁移必须逐项复制、复测，再更新本索引。
