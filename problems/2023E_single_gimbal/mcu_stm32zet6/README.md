# STM32ZET6 SINGLE 纯计算框架

本目录接收 `$MV,SINGLE`，解析半帧/粘包，运行安全状态机和 P/PD 建议值计算。它没有 Keil 工程、板级初始化、定时器、GPIO、PWM 或执行机构驱动，不能直接作为真实云台固件烧录。

`NO_SPOT/LOST/ERROR` 或 `OK=0` 时 `valid=0`，pan/tilt 立即归零。建议以整数毫单位通过 `$DBG,SINGLE` 观察，不使用 `%f`。

接入真实云台前必须单独审查机械限位、命令方向、slew rate、急停、独立供电、共地与光束遮挡。当前禁止控制真实云台、舵机、激光、小车和机械臂。
